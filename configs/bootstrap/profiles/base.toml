# configs/bootstrap/profiles/base.toml
# Universally safe DOTS baseline — NO optional AI providers or skill packs.
#
# Use: ./bootstrap.sh --profile base

[profile]
name = "base"
description = "Core shell UX only — safe on any machine"

with = []

open_apps = ["iTerm"]
