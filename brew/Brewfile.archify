# Optional: ./setup.sh --with archify
# Node for agent skills comes from default fnm (configs/node/default.toml).
# This Brewfile is intentionally empty of Node — kept so --with archify remains
# a valid brew-bundle no-op hook for future Archify-native deps.
# (Previously installed brew "node"; prefer fnm-managed Node.)
